#include "TacoBellNode.h"

TacoBellNode::TacoBellNode(double latitude, double longitude, std::string address, int store_id) : 
    latitude_(latitude), longitude_(longitude), address_(address), store_id_(store_id) {}